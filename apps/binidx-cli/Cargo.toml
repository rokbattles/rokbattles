[package]
name = "binidx-cli"
version = "1.1.0"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
binidx-decoder = { path = "../../crates/binidx-decoder" }

[lib]
doctest = false
