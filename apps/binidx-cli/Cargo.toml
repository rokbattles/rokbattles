[package]
name = "binidx-cli"
version = "1.0.3"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
binidx-decoder = { path = "../../crates/binidx-decoder" }

[lib]
doctest = false
