import { notImplementedResponse } from "@/lib/api/not-implemented";

export function DELETE() {
  /**
   * Remove a report (by parent hash) from the current user's favorites.
   */
  return notImplementedResponse();
}
