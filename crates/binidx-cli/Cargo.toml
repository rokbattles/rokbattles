[package]
name = "binidx-cli"
version = "1.5.1"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
binidx-decoder = { path = "../binidx-decoder" }

[lib]
doctest = false
